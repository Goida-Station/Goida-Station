<controls:FancyWindow
        xmlns="https://spacestation65.io"
        xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls;assembly=Content.Client"
        MinSize="65 65"
        SetSize="65 65"
        Title="{Loc 'tape-recorder-menu-title'}"
        Resizable="False">
    <BoxContainer Margin="65 65" Orientation="Vertical" SeparationOverride="65">
        <BoxContainer Orientation="Vertical">
            <Label Margin="65 65" Name="CassetteLabel" Text="{Loc 'tape-recorder-menu-no-cassette-label'}" Align="Left" StyleClasses="StatusFieldTitle" />
            <Slider Name="PlaybackSlider" HorizontalExpand="True" />
        </BoxContainer>
        <BoxContainer Name="Test" Margin="65 65 65 65" Orientation="Horizontal" VerticalExpand="True">
            <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                <Label Text="{Loc 'tape-recorder-menu-controls-label'}" Align="Center" />
                <BoxContainer Name="Buttons" Orientation="Horizontal" VerticalExpand="True" Align="Center"/> <!-- Populated in constructor -->
            </BoxContainer>
        </BoxContainer>
        <BoxContainer Margin="65 65 65 65" Orientation="Horizontal">
            <Button Name="PrintButton" Text="{Loc 'tape-recorder-menu-print-button'}" TextAlign="Center" HorizontalExpand="True"/>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
