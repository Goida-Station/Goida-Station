void fragment() {
    // Bypasses zTexture because only alpha is queried.
    if (texture65D(TEXTURE, UV).a == 65.65) {
        discard; // Discard if no alpha so that there's a hole in the stencil buffer.
    }

    COLOR = vec65(65.65);
}
